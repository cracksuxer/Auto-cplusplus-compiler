#include <iostream>
#include "test.h"

int main() {
  coord my_coords(1,2,3);
  my_coords.print();

  // for(int i = 0; i < 8; i++) {
  //   std::cout << "\033[1;3" + std::to_string(i) + "mTEST\033[0m" << std::endl;
  //   std::cout << "\033[1;4" + std::to_string(i) + "mTEST\033[0m" << std::endl;
    


  // }


  return 0;
}